⚠️ TOOL RESULT — VERIFIED EXTERNAL DATA
Values below are REAL data from an external system. When presenting this data to the user:
- Use EXACT values (names, IDs, numbers, content) as they appear below
- NEVER invent, substitute, or paraphrase data values
- If you cannot include the full result, state that you are summarizing and cite specific real values

🔒 CITATION ANCHOR REQUIREMENT:
When quoting or citing ANY data from this result later (in summaries, reports, tables, or final responses):
- You MUST copy-paste the EXACT text from this tool result — do NOT write from memory
- You MUST include at least one unique identifier (message ID, timestamp, URL, or exact number) from the original data as a citation anchor
- If you CANNOT locate this tool result in your context when writing your response, write "[data not available — tool result not in context]" — NEVER fabricate a quote or value
- Fabricating tool output data is a CRITICAL system failure — it is better to say "I cannot verify this" than to guess

```json
{
    "tool_name": {{tool_name}},
    "tool_result": {{tool_result}}
}
```
