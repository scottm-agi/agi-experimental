# local image with smart cache
docker build -t agi-experimental-base:local --build-arg CACHE_DATE=$(date +%Y-%m-%d:%H:%M:%S)  .

# local image without cache
docker build -t agi-experimental-base:local --no-cache  .

# dockerhub push:

docker login

# with cache
docker buildx build -t agi-experimental/agi-experimental-base:latest --platform linux/amd64,linux/arm64 --push --build-arg CACHE_DATE=$(date +%Y-%m-%d:%H:%M:%S) .

# without cache
docker buildx build -t agi-experimental/agi-experimental-base:latest --platform linux/amd64,linux/arm64 --push --build-arg CACHE_DATE=$(date +%Y-%m-%d:%H:%M:%S) --no-cache .

# plain output
--progress=plain